/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'com.xmanius.ai',
  appName: 'Xmanius',
  webDir: 'www',
  bundledWebRuntime: false,
  android: {
    backgroundColor: '#000000'
  }
};

module.exports = config;
